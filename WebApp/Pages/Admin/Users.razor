@page "/admin/users"
@layout AdminLayout
@attribute [Authorize(Roles = "Admin")]

<PageTitle>Users - Admin Panel</PageTitle>

<div class="space-y-6">
    <div class="flex items-center justify-between">
        <h1 class="text-2xl font-bold text-gray-900">User Management</h1>
        <button class="px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition">
            + Add New User
        </button>
    </div>
    
    <div class="bg-white rounded-lg shadow p-6">
        <!-- Search and Filter Section -->
        <div class="mb-6 grid grid-cols-1 md:grid-cols-4 gap-4">
            <!-- Search Bar -->
            <div class="md:col-span-2">
                <label class="block text-sm font-medium text-gray-700 mb-2">Search</label>
                <div class="relative">
                    <input type="text" @bind="_searchTerm" @bind:event="oninput" 
                           class="w-full px-4 py-2 pl-10 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" 
                           placeholder="Search by name, email, student ID..." />
                    <svg class="absolute left-3 top-3 h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                    </svg>
                </div>
            </div>

            <!-- Sort By -->
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Sort By</label>
                <select @bind="_sortBy" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    <option value="name">Name</option>
                    <option value="studentId">Student ID</option>
                    <option value="session">Session</option>
                    <option value="email">Email</option>
                    <option value="created">Created Date</option>
                </select>
            </div>

            <!-- Status Filter -->
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Status</label>
                <select @bind="_selectedStatus" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    <option value="">All Status</option>
                    <option value="Active">Active</option>
                    <option value="Inactive">Inactive</option>
                    <option value="Suspended">Suspended</option>
                </select>
            </div>
        </div>

        <!-- Results Count -->
        <div class="mb-4 flex items-center justify-between">
            <p class="text-sm text-gray-600">
                Showing <span class="font-semibold">@FilteredAndSortedUsers.Count</span> of <span class="font-semibold">@_users.Count</span> users
            </p>
            @if (!string.IsNullOrWhiteSpace(_searchTerm) || !string.IsNullOrWhiteSpace(_selectedStatus))
            {
                <button @onclick="ClearFilters" class="text-sm text-blue-600 hover:text-blue-800">
                    Clear Filters
                </button>
            }
        </div>

        <!-- Users Table -->
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            Student ID
                        </th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            Name
                        </th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            Email
                        </th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            Session
                        </th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            Department
                        </th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            Status
                        </th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            Created At
                        </th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            Actions
                        </th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    @if (FilteredAndSortedUsers.Count == 0)
                    {
                        <tr>
                            <td colspan="8" class="px-4 py-8 text-center text-gray-500">
                                No users found matching your criteria.
                            </td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var user in FilteredAndSortedUsers)
                        {
                            <tr class="hover:bg-gray-50">
                                <td class="px-4 py-3 text-sm font-medium text-gray-900">@user.StudentId</td>
                                <td class="px-4 py-3 text-sm font-medium text-gray-900">@user.Name</td>
                                <td class="px-4 py-3 text-sm text-gray-500">@user.Email</td>
                                <td class="px-4 py-3 text-sm text-gray-500">@user.Session</td>
                                <td class="px-4 py-3 text-sm text-gray-500">@user.Department</td>
                                <td class="px-4 py-3 text-sm">
                                    <span class="inline-flex px-2 py-1 text-xs leading-5 font-semibold rounded-full
                                                   @(user.Status == "Active" ? "bg-green-100 text-green-800" : 
                                                     user.Status == "Suspended" ? "bg-yellow-100 text-yellow-800" :
                                                     "bg-red-100 text-red-800")">
                                        @user.Status
                                    </span>
                                </td>
                                <td class="px-4 py-3 text-sm text-gray-500">
                                    @user.CreatedAt.ToString("dd MMM yyyy")
                                </td>
                                <td class="px-4 py-3 text-sm">
                                    <button class="text-blue-600 hover:text-blue-800 mr-3">View</button>
                                    <button class="text-yellow-600 hover:text-yellow-800 mr-3">Edit</button>
                                    <button class="text-red-600 hover:text-red-800">Delete</button>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@code {
    public class UserRow
    {
        public string StudentId { get; set; } = default!;
        public string Name { get; set; } = default!;
        public string Email { get; set; } = default!;
        public string Session { get; set; } = default!;
        public string Department { get; set; } = default!;
        public string Status { get; set; } = default!;
        public DateTime CreatedAt { get; set; }
    }

    private List<UserRow> _users = new()
    {
        new UserRow 
        { 
            StudentId = "200101", 
            Name = "Md. Mamun-Or-Rashid", 
            Email = "200101.cse@student.just.edu.bd",
            Session = "2023-24",
            Department = "CSE",
            Status = "Active",
            CreatedAt = DateTime.Now.AddMonths(-3)
        },
        new UserRow 
        { 
            StudentId = "210152", 
            Name = "Heaven Chakma", 
            Email = "210152.cse@student.just.edu.bd",
            Session = "2021-22",
            Department = "CSE",
            Status = "Active",
            CreatedAt = DateTime.Now.AddMonths(-2)
        },
        new UserRow 
        { 
            StudentId = "200103", 
            Name = "Sarah Ahmed", 
            Email = "200103.cse@student.just.edu.bd",
            Session = "2022-23",
            Department = "CSE",
            Status = "Suspended",
            CreatedAt = DateTime.Now.AddMonths(-1)
        },
        new UserRow 
        { 
            StudentId = "200104", 
            Name = "Karim Rahman", 
            Email = "200104.cse@student.just.edu.bd",
            Session = "2023-24",
            Department = "CSE",
            Status = "Inactive",
            CreatedAt = DateTime.Now.AddMonths(-4)
        },
        new UserRow 
        { 
            StudentId = "200105", 
            Name = "Fatima Begum", 
            Email = "200105.cse@student.just.edu.bd",
            Session = "2022-23",
            Department = "CSE",
            Status = "Active",
            CreatedAt = DateTime.Now.AddMonths(-5)
        }
    };

    private string _searchTerm = string.Empty;
    private string _selectedStatus = string.Empty;
    private string _sortBy = "name";

    private List<UserRow> FilteredAndSortedUsers
    {
        get
        {
            var filtered = _users.AsEnumerable();

            // Search filter
            if (!string.IsNullOrWhiteSpace(_searchTerm))
            {
                filtered = filtered.Where(u => 
                    u.Name.Contains(_searchTerm, StringComparison.OrdinalIgnoreCase) ||
                    u.Email.Contains(_searchTerm, StringComparison.OrdinalIgnoreCase) ||
                    u.StudentId.Contains(_searchTerm, StringComparison.OrdinalIgnoreCase) ||
                    u.Session.Contains(_searchTerm, StringComparison.OrdinalIgnoreCase)
                );
            }

            // Status filter
            if (!string.IsNullOrWhiteSpace(_selectedStatus))
            {
                filtered = filtered.Where(u => u.Status == _selectedStatus);
            }

            // Sorting
            filtered = _sortBy switch
            {
                "studentId" => filtered.OrderBy(u => u.StudentId),
                "session" => filtered.OrderBy(u => u.Session).ThenBy(u => u.StudentId),
                "email" => filtered.OrderBy(u => u.Email),
                "created" => filtered.OrderByDescending(u => u.CreatedAt),
                _ => filtered.OrderBy(u => u.Name)
            };

            return filtered.ToList();
        }
    }

    private void ClearFilters()
    {
        _searchTerm = string.Empty;
        _selectedStatus = string.Empty;
        _sortBy = "name";
    }
}
